# CRAB Crawl Findings Ledger — 2026-07-16

Scope: read-only crawl of `C:\Users\Owner\PROJECTS\hummbl-bibliography` during a multi-agent session. Findings are evidence-backed and remain unassigned; no code or other agents' drafts were modified.

| ID | Class | Severity | Finding | Evidence | Suggested next action | Status |
|---|---|---:|---|---|---|---|
| CRAB-20260716-001 | Prior lead invalidated | — | Earlier probe incorrectly treated the root package as the workflow package. The workflow correctly runs inside `toolkit/`, where both scripts exist and pass. | `toolkit/package.json:39-40` defines `type-check` and `build`; `npm --prefix toolkit run type-check` and `npm --prefix toolkit run build` both exit 0. | Retain as a verification correction; do not file as a CI bug. | Closed — false positive |
| CRAB-20260716-002 | Prior lead invalidated | — | Earlier claim that `monitorModels.js` could not be rebuilt was incorrect after verifying the toolkit build. | `toolkit/tsconfig.json:13-18` emits `dist`; `npm --prefix toolkit run build` exits 0 and produces `dist/utils/monitorModels.js`. | Retain as a verification correction; do not file as a CI bug. | Closed — false positive |
| CRAB-20260716-003 | Documentation drift | Medium | `CI_HEALTH.md` reports npm audit as blocking CI and says PR #65 still needs merging, while the live workflow is already warn-only. | `CI_HEALTH.md:7-22` says audit is hard-failing and PR #65 is pending; `.github/workflows/ci.yml:62-70` has `continue-on-error: true` and warning behavior. Document last updated 2026-06-19. | Refresh the document against current workflow/run state and remove stale “action needed” language. | Open |
| CRAB-20260716-004 | Workspace hygiene debt | Medium | Active worktree contains numerous untracked temporary reports, scripts, backups, and adversarial fixtures, increasing accidental-inclusion and review-noise risk. | `git status --short` in `C:\Users\Owner\PROJECTS\hummbl-bibliography` reports at least `.gitleaks.toml.bak`, multiple `tmp-*.json`, `tmp-*.js`, `tmp-*.md`, `tmp-gitleaks-test/`, and an untracked adversarial test. | Assign an owner to classify each artifact as retain, gitignore, relocate, or delete; do not bulk-delete while other agents are active. | Open |
| CRAB-20260716-005 | CI blocker | High | The validation workflow's README badge check will fail against the current corpus count. | `.github/workflows/validate.yml:43-52` compares the README badge to generated stats; `README.md:3` advertises `entries-260`, while `npm --prefix toolkit run stats:json` reports `total: 321`. | Update the badge after confirming the intended canonical corpus count, or change the workflow to use the authoritative count source. Add a regression test for the badge/count contract. | Open |

## Verification receipt

- Repository: `fix/codex/bibliography-schema-registry-audit-fix` at `a782f4b`.
- `npm test`: PASS — 94 tests, 0 failures.
- `npm --prefix toolkit run type-check`: PASS.
- `npm --prefix toolkit run build`: PASS.
- Root-level `npm run type-check` / `npm run build` fail because the root delegation shim does not expose those scripts; this is not a CI failure because workflows enter `toolkit/`.
- No commits, pushes, or code fixes made by this crawl.

## Cross-check addendum — 2026-07-16 07:15 EDT

This addendum preserves the original crawl rows and records a separate verification pass. It corrects only claims contradicted by direct workflow execution evidence.

| ID | Class | Severity | Finding | Evidence | Suggested next action | Status |
|---|---|---:|---|---|---|---|
| XCHK-20260716-001 | Cross-check confirmation | — | Confirms the corrected status of `CRAB-001` and `CRAB-002`: `validate-models.yml` changes into `toolkit` before both commands, where `type-check` and `build` are defined; its `dist/utils/monitorModels.js` import is generated by that build. | `.github/workflows/validate-models.yml:40-54`; `npm --prefix toolkit run type-check` = 0; `npm --prefix toolkit run build` = 0; `toolkit/src/utils/monitorModels.ts` compiles to `toolkit/dist/utils/monitorModels.js`. Root-level `npm run type-check`/`build` fail because the root package is intentionally a delegation shim. | Keep rows 001/002 closed unless a clean-checkout CI run later proves a distinct failure. | Confirmed |
| CRAB-20260716-006 | CI blocker | High | GitHub Actions execution is unavailable/degraded, preventing server-side validation and merge automation. The repository’s active outage policy requires manual validation and an explicit hold; live PR #120 checks all terminate before any job step. | `docs/GITHUB_ACTIONS_BILLING_OUTAGE_POLICY.md:3-14,40-49`; PR #120 run `29478121861` completed in 4 seconds with six failed/cancelled checks and empty step lists; scheduled Dependabot run `29487066343` also failed before steps. | Maintain `CI_UNAVAILABLE_BILLING`/hold status. Restore Actions billing/execution, then rerun the affected workflows; do not interpret missing checks as passes. | Open |
| CRAB-20260716-007 | CI blocker | High | The only registered self-hosted runner, `wsl-bib`, is offline. Main/push/scheduled CI, validation, and security-audit workflows select that runner for non-PR events. | GitHub Actions runners API: `wsl-bib` status `offline`, labels `self-hosted,Linux,X64,wsl,bibliography`; `.github/workflows/ci.yml:25,85`, `validate.yml:15`, and `security-audit.yml:19` select those labels outside `pull_request`. | Restore and health-check `wsl-bib`, then rerun a non-PR workflow. This does not explain the separate Ubuntu-hosted pre-step failures in CRAB-006. | Open |
| CRAB-20260716-008 | Security/CI blocker | High | Gitleaks is not actionable against repository content: the configured `generic-api-key` rule flags citation/semantic `key` values. The existing tracked-artifact issue records 264 false positives; a local whole-tree scan returned 691 findings, including untracked adversarial material, so that larger count is not yet fully triaged. | Issue #109; `.gitleaks.toml` only allowlists tests, hook rules, and workflow paths; local `gitleaks detect --no-git --config .gitleaks.toml` exit 1. | Add the narrowest rule-scoped, path-anchored exception for documented citation-key data and adversarial tests proving actual secrets still fail. Do not disable `generic-api-key` globally. | Open |
| CRAB-20260716-009 | Security/CI blocker | High | Security Audit cannot retain its audit artifact because GitHub artifact storage quota is exhausted. This is an infrastructure failure after audit production, not a zero-vulnerability result. | Open issue #108 records `Failed to CreateArtifact: Artifact storage quota has been hit.`; local `npm --prefix toolkit audit --omit=dev --audit-level=high` found 0 vulnerabilities. | Choose and document a retention/compaction policy, then verify artifact upload after capacity is restored. Do not silently downgrade the audit. | Open |
| CRAB-20260716-010 | Documentation drift | Medium | Canonical-facing documentation and agent instructions report 260 entries across 13 tiers, while the current validator reports 321 entries across 19 files; open PR #107 describes yet another observed total (345). | `README.md:3,11,48,100-112`; `SITREP.md:17-25`; `AGENTS.md:4,7,31`; `npm run validate` output: `Total Entries: 321`. | Establish one generated statistics source and update README, SITREP, AGENTS instructions, badges, and PR #107 against the same revision. | Open |
| CRAB-20260716-011 | Metadata debt | Medium | The validated corpus has 62 missing DOI/ISBN warnings: 185/321 entries have a DOI and 72/321 have an ISBN. This makes published coverage claims stale and leaves roughly one fifth of entries without a persistent identifier. | `npm test` and `npm run validate` both report 321 entries, 62 warnings; duplicate/required-field checks pass. | Triage each warning as enrichable, intentionally identifier-less, or requiring an explicit `No DOI/ISBN available` annotation. | Open |
| CRAB-20260716-012 | Documentation/generated-artifact drift | Medium | Several secondary artifacts still describe the pre-expansion 260-entry corpus, while no obvious repository generator exists for `llms.txt`. | `llms.txt:3`, `CONTRIBUTING.md:7`, `CHANGELOG.md:41-52`, and `docs/GAP_ANALYSIS.md:5,9,85,92` retain 260-entry claims; `SITREP.md:66-67` now flags `llms.txt` for regeneration. | Identify or create a reproducible `llms.txt` generation path, regenerate it from canonical BibTeX, then update secondary documents from the same stats revision. | Open |
| CRAB-20260716-013 | Governance/documentation debt | Medium | `AGENTS.md` still defines the project as 260 entries across 13 tiers, conflicting with the validated 321-entry/19-file corpus. Editing this file is a governance change and should not be folded into routine documentation cleanup. | `AGENTS.md:4,7,31`; validator reports 321 entries; ledger CRAB-010 records the conflict. | Route a separate peer-reviewed governance/doc update; preserve the current instructions until the operator adopts the new corpus model. | Open — protected surface |
| CRAB-20260716-014 | Contributor workflow defect | Medium | Contributor instructions direct users to run a root-level `npm run build` command that does not exist. | `CONTRIBUTING.md:44-47` says `npm run build`; local execution exits 1 with `Missing script: build`. The working command is `npm --prefix toolkit run build`. | Correct the contributor command and update adjacent stale quality metrics in the same reviewed documentation change. | Open |

### Cross-check verification receipt

- Branch/HEAD: `fix/codex/bibliography-schema-registry-audit-fix` / `a782f4b` (five commits ahead of `origin/main`; no tracked edits before this ledger update).
- Passed: `npm test` (94 tests), `npm run validate` (0 errors, 62 warnings), `npm run check-dups` (0 duplicates), `npm --prefix toolkit run type-check`, `npm --prefix toolkit run build`, and `npm --prefix toolkit audit --omit=dev --audit-level=high` (0 vulnerabilities).
- Known limitation: all results were obtained locally; GitHub Actions was unavailable/degraded and no server-side check was treated as passing.
